# Pro版本第一阶段实施记录

## 阶段目标

本阶段目标是先打稳 Pro 版本底座：完成 MySQL 建表、SQLite 数据迁移、`info-serve` Go 服务基础骨架，并为后续用户鉴权和管理后台拆分预留清晰边界。

## 已完成

- 保存完整 MySQL 建表语句：`info_aggregation/sql/mysql_schema_pro.sql`。
- 所有新建业务字段均保留中文注释，覆盖内容采集、事件聚合、用户鉴权、用户偏好、管理审计、采集监控和质量快照。
- 新增 SQLite 到 MySQL 迁移脚本：`info_aggregation/sql/mysql_migration_from_sqlite.py`。
- 迁移脚本支持首次建库、可重复清库重跑、日期格式归一化、历史脏外键过滤。
- 新增 `info-serve` Go 服务，当前包含：
- `GET /health`
- `POST /api/auth/register`
- `POST /api/auth/login`
- `POST /api/auth/logout`
- `GET /api/me`
- `GET /api/admin/health`
- 环境配置加载
- 密码哈希与校验
- 统一 JSON 响应
- MySQL 用户与会话存储
- 管理员账号初始化命令
- 用户侧事件分类、事件列表、事件详情只读 API
- Pro 管理后台 `info-admin` 第一版 PC Web 静态骨架
- 管理后台登录面板，接入 `POST /api/auth/login`
- 管理后台总览接口 `GET /api/admin/overview`，要求管理员角色访问
- 管理总览已接入 MySQL 统计：渠道数、事件数、内容数、数据质量指标、最近采集运行
- 管理监控列表 API：
- `GET /api/admin/crawl-runs`
- `GET /api/admin/quality-snapshots`
- `GET /api/admin/crawl-tasks`
- `info-admin` 已渲染采集运行列表、质量快照列表、采集任务列表，空数据会显示明确状态
- `info_aggregation` 调度器已接入 Pro 监控表：
- 启动时同步 `crawl_task`
- 每次渠道采集写入 `crawl_run_log`
- 分类采集完成后写入 `data_quality_snapshot`
- 本地 MySQL 已初始化 12 个采集任务，并生成 1 条质量快照
- 管理接口已接入操作审计：
- 通过管理员/运营角色校验后的 `/api/admin/*` 请求会写入 `admin_audit_log`
- 审计字段包含管理员ID、动作、目标类型、目标ID、IP地址
- 配置管理已从 `info-max` 用户端删除，并迁移到 `info-serve + info-admin`：
- `GET /api/admin/categories`
- `POST /api/admin/categories`
- `PUT /api/admin/categories/{id}`
- `GET /api/admin/channels`
- `POST /api/admin/channels`
- `PUT /api/admin/channels/{id}`
- `info-admin` 已提供分类和渠道的后台管理入口，要求管理员登录后使用。
- 已识别当前 `info-admin` 静态原型不符合正式工程规范，后续将重建为 Vue3 + TypeScript 独立后台。
- 已补充 `info-serve` Go 服务架构设计，明确 handler、service、repository、domain、transport 分层边界。
- 新增架构治理文档：`docs/开发文档/2026-04-22-info-admin管理后台架构设计.md`、`docs/开发文档/2026-04-22-info-serve服务架构设计.md`、`docs/开发文档/2026-04-22-Pro架构治理实施计划.md`。
- `info-admin` 已完成工程化重建：
- 删除中文命名静态原型文件。
- 新增 Vue3 + TypeScript + Vite 工程。
- 新增登录、总览、采集监控、数据质量、分类管理、渠道管理、审计日志页面。
- 新增统一 HTTP 客户端、管理员登录态、路由守卫、后台 API 封装。
- `info-serve` 已完成架构守正第一步：
- 新增 `internal/app`，集中服务装配和依赖注入。
- `cmd/server/main.go` 已收敛为轻量启动入口。
- `response` 已补充 404 和 500 响应能力。
- handler 未知服务错误不再统一伪装成 400。
- `info-serve` 已完成 HTTP 层迁移准备：
- 新增 `internal/transport/http` 路由入口。
- `internal/app` 已直接依赖新 HTTP transport。
- 历史 `internal/router` 包已移除，路由行为测试已迁入 `internal/transport/http`。
- `health` 和 `auth` handler 已迁入 `internal/transport/http`。
- `events` handler 已迁入 `internal/transport/http`。
- 管理后台 handler 已迁入 `internal/transport/http`，`internal/handler` 已不再承载业务文件。
- 管理鉴权中间件已迁入 `internal/transport/http/middleware`，`internal/middleware` 已不再承载业务文件。
- 更新 `info_aggregation/deploy.env.example` 为 Pro MySQL 本地配置。
- 新增 `info-serve/deploy.env.example` 和 `info-serve/README.md`。

## MySQL迁移结果

本地 MySQL 地址：`localhost:3306/info-max`。

迁移结果：

- `category`：6 条。
- `channel`：12 条。
- `info`：611 条。
- `info_acquisition_log`：591 条。
- `event`：199 条。
- `event_item_link`：200 条。
- `event_timeline_entry`：200 条。
- `event_summary_snapshot`：796 条。

Pro 新增表已创建，当前无历史数据：

- `user_session`
- `user_favorite_event`
- `user_follow_keyword`
- `user_preference`
- `user_read_history`
- `admin_audit_log`
- `crawl_task`
- `crawl_run_log`
- `crawl_health_snapshot`
- `data_quality_snapshot`

`user_account` 已通过 `info-serve/cmd/create-admin` 初始化本地管理员账号：

- 邮箱：`admin@info-daren.local`
- 角色：`admin`
- 说明：该账号仅用于本地 Pro 开发阶段验证管理接口强制登录，后续进入正式环境前必须重新设置安全密码。

## 验证记录

已通过验证：

```bash
cd /Users/jeasonwang/IdeaProjects/info-ai/info_aggregation
./.venv/bin/python -m pytest tests/test_mysql_schema_pro.py tests/test_mysql_migration_script.py -q
```

结果：7 个测试通过。

```bash
cd /Users/jeasonwang/IdeaProjects/info-ai/info-serve
GOCACHE=/tmp/info-serve-go-build-cache go test ./...
```

结果：Go 服务测试通过。

## 后续任务

- 将 `info-admin` 的审计日志列表接入后台页面。
- 将 `info-max` 用户侧 API 从旧 FastAPI 平滑切换到 `info-serve`。
- 增加管理后台手动触发采集、单渠道重采、低质量数据治理入口。
- 以测试驱动方式补充 `/api/v1/*` 等价入口，先与 `/api/*` 并行一个版本周期，再推动前端统一切换。
