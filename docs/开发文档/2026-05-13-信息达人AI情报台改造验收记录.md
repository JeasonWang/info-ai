# 2026-05-13 信息达人 AI 情报台改造验收记录

## 本轮目标

按 `docs/superpowers/plans/2026-05-13-info-daren-ai-intelligence-upgrade.md` 开始第一批快速改造，优先建立数据质量基线、低价值内容识别、事件展示质量门槛和规则分析防劣化能力。

## 已完成改造

1. 新增质量审计工具：
   - `info_aggregation/tools/event_quality_audit.py`
   - 支持输出渠道采集质量、事件摘要质量、分析运行质量和 LLM 调用质量。
2. 新增低价值内容识别：
   - 识别 `hot/new`、互动计数、热榜分类、纯表情、纯编号、Reuters sitemap 元数据等不能作为事实依据的文本。
3. 放宽标题清洗与 crawler 标题截断：
   - `clean_title()` 从 40 字放宽到 200 字。
   - 各 crawler 的 `title[:40]` / `word[:40]` 调整为 200 字上限，避免英文标题半截话。
4. 新增事件展示质量门槛：
   - `display_quality_score`
   - `display_quality_level`
   - `display_quality_reason`
   - 弱社交热度信号进入 `monitoring`，非社交事实来源仍可进入 `active`。
5. 规则分析防低价值摘要：
   - 规则分析不再把 `互动：点赞xx` 作为一句话摘要。
   - 弱来源输出“热度线索 / 缺少完整事实来源”的谨慎文案。
6. LLM 事件分析审计增强：
   - 事件分析 LLM 调用会把 request payload、response content、response payload 写入 `llm_call_log`。
   - 后续管理端可以基于日志抽检坏摘要对应的 prompt、模型返回和来源上下文。
7. `info-serve` API 输出增强：
   - 事件列表和详情透出 `display_quality_score`、`display_quality_level`、`display_quality_reason`。
   - `info-mvp` 类型定义已补充这些字段，后续可用于卡片可信度展示。
8. 新增数据库迁移：
   - `info_aggregation/sql/migration_v1.5.0_event_display_quality.sql`
   - 本地 MySQL `info-max.event` 已执行迁移。
9. 新增历史事件展示质量回填：
   - `info_aggregation/tools/backfill_event_display_quality.py`
   - 可对 `active/monitoring/low_quality` 事件批量重算展示质量，并按结果分流到 `active` 或 `monitoring`。
10. 新增 LLM 事件分析轻量重试：
   - 新增 `EVENT_ANALYSIS_LLM_RETRY_TIMES`，默认每次事件分析最多尝试 2 次。
   - 瞬时 timeout 或临时无效返回会先重试；最终仍失败时才记录失败日志并回退规则分析。
   - 避免一次临时网络/模型抖动直接让高价值事件退回规则摘要。
11. 管理端事件展示质量可解释治理：
   - `event-analysis-quality-report` 增加展示质量分流统计：追踪事件、可展示、已拦截、展示可用率、状态分布、等级分布、拦截原因 Top。
   - 管理端“事件分析质量”页增加展示质量拦截样本，直接显示拦截状态、展示分、等级、原因和更新时间。
   - 运营可以直接判断事件是“分析质量差”还是“来源质量弱导致不展示”。
12. 跨源事件聚合补强：
   - 在事件构建阶段增加同分类相似锚点合并。
   - 对没有 `core_entity/tech_entities/tech_keywords` 的社会热点，使用标题/正文共享线索进行保守合并。
   - 覆盖“广西公交车坠翻致3死5伤”和“广西梧州公交事故救援进展”这类不同标题写法但同一事件的跨源聚合。
13. Reuters 官方 sitemap 质量补强：
   - sitemap 采集增加栏目、发布时间、图片说明、股票代码、Reuters news code 等官方结构化上下文。
   - 详情补采支持识别已入库的 Reuters sitemap metadata，不再依赖 `_reuters_source` 临时字段。
   - 新增 `info_aggregation/tools/backfill_reuters_sitemap_metadata.py`，可无网络、无节流回填历史 Reuters metadata 质量字段。
14. 用户端展示质量表达：
   - `EventList` 卡片显示“高可信 / 可信 / 观察 / 质量分”。
   - 事件详情头部显示展示质量等级和主要原因，让用户知道当前结论的可信程度。
15. 微博/小红书事实源校验：
   - 社交平台即使抓到较长正文，也必须包含官方、通报、警方、媒体、事故、数据、报告等事实锚点，才可能进入 `active`。
   - 纯网友讨论、穿搭、互动热度、粉丝关注等内容会进入 `monitoring`，避免用户端首页被“有热度但无事实”的线索污染。

## 真实库基线

执行命令：

```bash
cd info_aggregation
./.venv/bin/python tools/event_quality_audit.py --pretty
```

关键结果：

| 指标 | 当前值 |
|---|---:|
| active 事件数 | 1893 |
| active 短摘要数 | 28 |
| active 短摘要比例 | 1.48% |
| active 低价值摘要数 | 1196 |
| active 低价值摘要比例 | 63.18% |
| active 单来源事件数 | 1754 |
| active 单来源事件比例 | 92.66% |
| LLM 调用数 | 203 |
| LLM 失败数 | 45 |
| LLM 失败比例 | 22.17% |
| 最新 LLM 失败原因 | The read operation timed out |

渠道风险摘要：

| 渠道 | 总数 | complete | list_only | pending | 完整率 |
|---|---:|---:|---:|---:|---:|
| weibo | 1509 | 175 | 36 | 658 | 11.60% |
| xiaohongshu | 2110 | 810 | 33 | 969 | 38.39% |
| reuters | 495 | 0 | 464 | 0 | 0.00% |
| toutiao | 386 | 147 | 39 | 0 | 38.08% |
| 36kr | 191 | 144 | 20 | 0 | 75.39% |
| csdn | 132 | 132 | 0 | 0 | 100.00% |
| juejin | 99 | 93 | 0 | 0 | 93.94% |
| cnblogs | 20 | 20 | 0 | 0 | 100.00% |

## 真实库回填结果

执行命令：

```bash
cd info_aggregation
./.venv/bin/python tools/backfill_event_display_quality.py --pretty
./.venv/bin/python tools/event_quality_audit.py --pretty
```

回填结果：

| 指标 | 值 |
|---|---:|
| 处理事件数 | 1931 |
| 变更事件数 | 1931 |
| 回填后 active 事件数 | 687 |
| 回填后 monitoring 事件数 | 1244 |

回填后审计关键结果：

| 指标 | 回填前 | 回填后 |
|---|---:|---:|
| active 事件数 | 1893 | 687 |
| active 短摘要比例 | 1.48% | 1.46% |
| active 低价值摘要数 | 1196 | 150 |
| active 低价值摘要比例 | 63.18% | 21.83% |
| active 单来源事件数 | 1754 | 669 |
| active 单来源事件比例 | 92.66% | 97.38% |

说明：本轮回填优先解决“明显低价值热点卡片直接出现在用户端”的问题，因此 active 总量明显收缩；剩余 active 单来源比例仍高，说明下一批重点应继续补强跨源聚合、Reuters 详情采集和微博/小红书事实源校验，而不是只靠展示门槛兜底。

## 已验证测试

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q
```

结果：`230 passed, 2 warnings`

2026-05-13 回填工具补充后复测：

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q
```

结果：`231 passed, 2 warnings`

2026-05-13 LLM 重试补充后复测：

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q
```

结果：`232 passed, 2 warnings`

2026-05-13 管理端展示质量治理补充后复测：

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q
```

结果：`233 passed, 2 warnings`

```bash
cd info-admin
npm test -- --run
npm run build
```

结果：`4 passed, 9 tests passed`，生产构建通过。

2026-05-13 跨源聚合补强后复测：

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q
```

结果：`234 passed, 2 warnings`

2026-05-13 Reuters sitemap metadata 补强后复测与回填：

```bash
cd info_aggregation
./.venv/bin/python tools/backfill_reuters_sitemap_metadata.py --pretty
./.venv/bin/python tools/event_quality_audit.py --pretty
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q
```

回填结果：`processed_count=495`，`changed_count=495`，`status_counts.partial=495`。

关键变化：

| 指标 | 补强前 | 补强后 |
|---|---:|---:|
| Reuters 总数 | 530 | 530 |
| Reuters complete | 4 | 4 |
| Reuters partial | 31 | 526 |
| Reuters list_only | 495 | 0 |
| Reuters failed/pending | 0 | 0 |

说明：当前环境访问 Reuters 正文页和文章 API 仍会返回 401/403，因此本轮先把官方 sitemap 结构化信息提升为可用的 partial 详情，解决“只有标题/URL 元数据”的问题；完整正文仍需要后续引入授权、代理或渲染策略。

测试结果：`235 passed, 2 warnings`

2026-05-13 用户端展示质量表达补充后验证：

```bash
cd info-mvp
npm run verify:h5
npm run verify:mp-weixin
```

结果：H5 和微信小程序构建均通过。

2026-05-13 社交事实源校验与真实库回填：

```bash
cd info_aggregation
./.venv/bin/python tools/backfill_event_display_quality.py --pretty
./.venv/bin/python tools/event_quality_audit.py --pretty
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q
```

回填结果：`processed_count=2062`，`changed_count=626`，回填后 `active=593`，`monitoring=1469`。

关键变化：

| 指标 | 回填前 | 回填后 |
|---|---:|---:|
| active 事件数 | 818 | 593 |
| active 低价值摘要数 | 209 | 110 |
| active 低价值摘要比例 | 25.55% | 18.55% |
| active 短摘要比例 | 1.59% | 1.85% |

测试结果：`237 passed, 2 warnings`

```bash
cd info-serve
env GOCACHE=/tmp/info-serve-go-build-cache go test ./...

cd ../info-mvp
npm run verify:h5
npm run verify:mp-weixin
```

结果：Go 服务、H5 和微信小程序构建均通过。

```bash
cd info_aggregation
./.venv/bin/python -m pytest \
  tests/test_data_quality_gate.py \
  tests/test_event_quality_audit.py \
  tests/test_event_display_quality.py \
  tests/test_event_analysis_pipeline.py \
  tests/test_event_rebuild_flow.py::test_rebuild_events_uses_latest_infos_when_table_exceeds_limit \
  -q
```

结果：`19 passed, 2 warnings`

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest \
  tests/test_event_api.py::test_list_events_returns_grouped_cards \
  tests/test_event_api.py::test_rebuild_events_prefers_high_quality_source_as_event_lead \
  tests/test_event_api.py::test_get_event_returns_timeline_and_summaries \
  -q
```

结果：`3 passed, 2 warnings`

```bash
cd info-serve
env GOCACHE=/tmp/info-serve-go-build-cache go test ./...
```

结果：全部通过。

```bash
cd info-admin
npm test -- --run
```

结果：`4 passed, 9 tests passed`

```bash
cd info-mvp
npm run verify:h5
npm run verify:mp-weixin
```

结果：H5 和微信小程序构建均通过。

## 下一步

2026-05-13 观察中事件产品出口：

```bash
cd info-serve
env GOCACHE=/tmp/info-serve-go-build-cache go test ./...

cd ../info-mvp
npm run verify:h5
npm run verify:mp-weixin
```

改造内容：

1. `info-serve` 事件列表新增 `status=active|monitoring` 查询能力，默认仍为 `active`，保持现有主信息流稳定。
2. 事件列表与详情返回 `status`，用户端可以区分可信事件和观察中事件。
3. `info-mvp` 首页新增“可信事件 / 观察中”分段入口，观察中事件以谨慎视图承接被质量门控拦下但仍有热度的线索。

真实库抽样：

| 状态 | 数量 | 平均展示质量分 |
|---|---:|---:|
| active | 604 | 51.22 |
| monitoring | 1469 | 18.50 |
| archived | 2459 | 0.00 |

测试结果：Go 服务、H5 和微信小程序构建均通过。

## 下一步

2026-05-13 事件重建真实库预演与过度合并修正：

```bash
cd info_aggregation
./.venv/bin/python tools/event_rebuild_preview.py --limit 300 --pretty
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q
```

预演发现：旧的相似标题补充分组规则过宽，会把社交平台上无关标题合并进同一事件组，不适合直接做真实库小批量重建。

修正策略：

1. 增加只读预演工具 `tools/event_rebuild_preview.py`，用于观测最新数据的候选分组、单条组比例、多源组比例和既有事件命中状态，不写库。
2. 收紧无实体字段标题合并规则：仅在标题相似度足够高，或双方标题存在共享词且双方都具备事件线索词时才合并。
3. 补充回归测试，保证事故类跨源标题仍能合并，同时无关社交标题不会被误合并。

预演对比：

| 指标 | 收紧前 | 收紧后 |
|---|---:|---:|
| 最新样本数 | 300 | 300 |
| 候选事件组 | 25 | 220 |
| 单条组比例 | 68.00% | 85.45% |
| 多来源组比例 | 12.00% | 0.91% |

说明：这一步刻意转向保守聚合，先解决“错合并”风险；后续再通过结构化实体、官方事实源和专题级聚合提高“该合并的合并率”。

测试结果：`238 passed, 2 warnings`

## 下一步

2026-05-13 用户端核心产品形态收敛：

```bash
cd info-mvp
npm run verify:h5
npm run verify:mp-weixin
```

改造目标：不新增复杂功能，只把用户端从普通热点列表推进为“可信热点情报台”。

改造内容：

1. 首页新增“今日重点 / 重点观察”情报区，基于当前筛选结果展示前三条最值得先看的事件。
2. 首页保留“可信事件 / 观察中”双入口，默认可信事件，观察中用于承接有热度但事实源不足的线索。
3. 事件卡片新增“优先看 / 多源确认 / 待核实 / 线索”判断标签，并补充观察原因或判断依据。
4. 事件详情页新增“情报摘要”区，把核心事实、最新进展、为什么重要、风险提示集中展示在详情首屏。
5. 详情页增加事件状态判断，例如“多来源交叉验证”或“观察中，等待更多事实源”。

验证结果：H5 和微信小程序构建均通过。

## 下一步

2026-05-13 核心能力收敛与 LLM Runner 抽离：

原则调整：近期不再横向增加功能入口，优先建设三项核心能力：

1. 采集完整性：让核心信源的正文、时间、来源、图片/元数据尽量完整。
2. 事件分析可信度：让标题、一句话总结、事实经过、风险提示稳定可用。
3. 大模型分析内核：让模型调用、prompt、解析、校验、fallback 架构清楚，方便后续快速迭代。

架构改动：

1. 新增 `services/event_analysis/llm_runner.py`，统一负责大模型事件分析的执行、重试、结果归一化和结果校验。
2. `pipeline.py` 收敛为编排层，只处理是否启用模型、模型配置选择、调用日志记录和规则 fallback。
3. 结合已拆出的 `prompt_builder.py` 和 `result_parser.py`，大模型事件分析核心链路变为：Prompt 构建 → Provider 调用 → Runner 校验 → Pipeline 记录与 fallback。

验证：

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest \
  tests/test_event_analysis_llm_runner.py \
  tests/test_event_analysis_prompt_builder.py \
  tests/test_llm_call_logging.py \
  tests/test_event_analysis_pipeline.py \
  -q
```

结果：`13 passed`

## 下一步

2026-05-13 RuoYi 风格后台增强与大模型事件分析封装：

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest \
  tests/test_event_analysis_prompt_builder.py \
  tests/test_llm_call_logging.py \
  tests/test_event_analysis_pipeline.py \
  -q

cd ../info-admin
npm test -- --run
npm run build
```

后台改造：

1. 数据质量页增加 RuoYi/Element 风格的渠道质量表格，展示渠道、完整率、可用率、待治理数量和凭证状态。
2. 表格引入进度条表达，运营可以快速判断哪个渠道采集详情不足。
3. 事件分析质量页增加风险分布卡，展示缺失分析、低置信度、模型回退、展示拦截，辅助判断分析链路瓶颈。

大模型事件分析架构调整：

1. 新增 `EventAnalysisPromptBuilder`，专门负责事件分析任务指令、来源裁剪、历史上下文注入和日志上下文。
2. 新增 `EventAnalysisResultParser`，专门负责把模型 JSON 输出解析成内部 `EventAnalysisResult`。
3. Provider 只保留模型调用和适配职责，后续扩展摘要优化、事实校验、事件关联、专题生成时，可以复用同一套模型调用层，并新增不同任务的 prompt/parser。

大模型在事件分析中的定位：

| 能力 | 本地规则职责 | 大模型职责 |
|---|---|---|
| 一句话总结 | 去噪、兜底、完整句校验 | 生成更自然、更有判断力的摘要 |
| 事实经过 | 基于来源抽取明确句子 | 归纳多来源共同事实，标注不确定性 |
| 价值判断 | 来源数、完整度、质量分 | 解释为什么值得看、可能影响什么 |
| 事件关联 | 稳定键、实体、标题相似度 | 判断多个热点是否属于同一脉络或专题 |
| 风险控制 | 低价值检测、展示门控、fallback | 不确定时明确保守表达，不替代事实源 |

测试结果：Python 定向测试 `11 passed`，`info-admin` 单测 `9 passed`，后台生产构建通过。

## 下一步

2026-05-14 三周计划核心闭环推进：

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q

cd ../info-serve
env GOCACHE=/tmp/info-serve-go-build-cache go test ./...

cd ../info-admin
npm test -- --run
npm run build

cd ../info-mvp
npm run verify:h5
npm run verify:mp-weixin
```

核心能力完成情况：

1. 采集质量治理：核心信源已形成统一质量指标，后台能看到 complete、partial、list_only、failed、pending、可用率和核心信源优先治理表。
2. Reuters 详情补强：正文访问受限时保留 `http_401_blocked/http_403_blocked/http_404_not_found` 等明确失败原因；列表 fallback 不再吞掉原始失败原因。
3. 事件聚合预演：预演工具新增 `risk_groups`，用于暴露疑似错合并或单源重复堆积；事件构建规则已收紧，优先避免无关热点串台。
4. LLM 事件分析：Prompt contract、Runner 校验和 Pipeline fallback 已闭环；一句话总结会拦截半截句、过短句、低价值句和照抄标题。
5. 用户端产品形态：首页收敛为“信息达人 AI 情报台”，卡片突出可信度、来源数、最新变化；详情页突出情报摘要、状态判断和风险提示。
6. 观察中事件：不再作为垃圾桶，而是解释为“有热度但证据不足”的待核实线索池。
7. 后台治理台：数据质量页和 LLM 模型页补充 RuoYi 风格表格、进度条、健康概览和熔断状态，方便运营判断瓶颈。
8. 本地联调：`scripts/start-local.sh` 默认开启 aggregation public API，等待 `/health`，并输出后台、用户端和健康检查入口；新增 3 周版本验收清单。

最新验证结果：

| 模块 | 命令 | 结果 |
|---|---|---|
| `info_aggregation` | `ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q` | `249 passed, 2 warnings` |
| `info-serve` | `env GOCACHE=/tmp/info-serve-go-build-cache go test ./...` | 通过 |
| `info-admin` | `npm test -- --run` / `npm run build` | `9 passed`，构建通过 |
| `info-mvp` | `npm run verify:h5` / `npm run verify:mp-weixin` | H5 和微信小程序构建通过 |
| 本地脚本 | `bash -n scripts/start-local.sh scripts/stop-local.sh` | 语法检查通过 |
| Compose 配置 | `docker compose config` | 通过 |

阶段判断：当前已经完成“核心信源可观测、事件可预演、LLM 可校验可回退、用户端有情报台形态、后台可治理”的主体闭环。后续优先做发布前文档和环境变量收口，再做真实数据小样本人工验收。

2026-05-14 本地四服务真实联调补丁：

本地服务启动后，抽样 `active` 事件流发现两个产品级问题：

1. 历史错合并事件仍可能留在数据库中，例如同一事件详情中混入“马化腾回应AI掉队”和“于正回应白鹿争议”，导致标题、摘要和来源串台。
2. 数据库中仍有历史遗留的 `active + weak/missing_complete_source` 事件，用户端可信事件流可能被旧质量字段污染。

修复内容：

1. `event_display_quality.py` 新增 `mixed_unrelated_sources` 识别：同一事件内多条标题明显无关时，事件降为 `monitoring`。
2. 用户端卡片和详情页新增该原因的中文解释：`来源疑似串台，等待重新拆分核验`。
3. 后台展示质量原因新增：`疑似错合并串台`。
4. `info-serve` 事件列表 API 对 `status=active` 增加兜底过滤：屏蔽 `weak/blocked`、`mixed_unrelated_sources`、`missing_usable_source`、`missing_complete_source` 的历史脏数据，避免可信事件流被旧数据污染。

验证：

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest \
  tests/test_event_display_quality.py \
  tests/test_event_api.py::test_related_grouping_does_not_merge_unrelated_developer_titles \
  -q

cd ../info-serve
env GOCACHE=/tmp/info-serve-go-build-cache go test ./...

cd ../info-admin
npm test -- --run
npm run build

cd ../info-mvp
npm run verify:h5
npm run verify:mp-weixin
```

结果：Python 定向测试 `9 passed`，Go 全量测试通过，后台 `9 passed` 且构建通过，用户端 H5 和微信小程序构建通过。

说明：由于当前运行中的本地服务仍是修复前进程，新规则需要重启 `info_aggregation` 和 `info-serve` 后才会在本地页面实时生效；历史数据还应执行一次展示质量回填。

2026-05-14 本地回填与可信流复验：

执行展示质量回填：

```bash
cd info_aggregation
./.venv/bin/python tools/backfill_event_display_quality.py --pretty
```

第一次回填结果：

```json
{
  "processed_count": 2408,
  "changed_count": 763,
  "status_counts": {
    "active": 245,
    "monitoring": 2163
  }
}
```

随后进一步收紧社交事实源判断：

1. 不再把 `回应`、`发布`、`宣布`、`记者`、`媒体` 这类普通词单独视为事实锚点。
2. 社交来源需要出现官方通报、执法/救援/事故/调查、权威媒体机构、数据报告等强事实信号，才可能进入可信事件。
3. `info-serve` 对 active 可信流增加摘要质量兜底过滤：过滤过短、半截句和明显旧规则坏摘要。

第二次回填结果：

```json
{
  "processed_count": 2408,
  "changed_count": 24,
  "status_counts": {
    "active": 235,
    "monitoring": 2173
  }
}
```

重启 `info_aggregation` 和 `info-serve` 后复验：

```bash
curl http://127.0.0.1:8000/health
curl http://127.0.0.1:8085/health
curl 'http://127.0.0.1:8085/api/v1/events?category_code=all&status=active&page=1&page_size=50'
```

复验结果：

| 检查项 | 结果 |
|---|---:|
| active 总数 | 165 |
| monitoring 总数 | 2173 |
| active 前 50 条 weak/blocked | 0 |
| active 前 50 条缺完整/可用来源 | 0 |
| active 前 50 条疑似错合并串台 | 0 |
| active 前 50 条过短/半截摘要 | 0 |
| hot 前 30 条坏样本 | 0 |
| international 前 30 条坏样本 | 0 |
| tech 前 30 条坏样本 | 0 |

阶段判断：可信事件流已经从“数量优先”收敛为“质量优先”。当前还存在的不足是：部分单源内容虽然通过可信门槛，但表达质量仍依赖后续事件分析补偿和大模型重写，下一步应继续做 active 样本的摘要重写与公共事件价值排序。

2026-05-14 全网默认流排序与社交单源收口：

继续真实验收时发现：可信流门槛收紧后，“全网”默认排序一度被最新技术长文占满；调整为公共事件优先后，又暴露出小红书生活/娱乐内容因为正文完整而进入热点 active。

修复内容：

1. `info-serve` 全网默认排序改为公共事件优先：国际事件、公共热点、财经、体育、科技，避免全网首页被技术文章流占满。
2. `info-serve` active 可信流增加社交单源兜底过滤：热点分类中，如果事件仅来自微博/小红书，默认不进入可信流。
3. `info_aggregation` 社交事实锚点继续收紧：移除 `根据`、`报告`、`事故` 等容易误触发的弱词，避免“观影报告”“根据人数”“汽车事故话题标签”等生活内容误判为事实源。
4. 保留强事实锚点：官方、通报、警方、消防、法院、应急、救援、伤亡、调查、声明、新华社、央视新闻、央视财经、央视网、财联社、澎湃、路透、Reuters、报道称、数据显示、调查报告。

验证：

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest tests/test_event_display_quality.py -q
./.venv/bin/python tools/backfill_event_display_quality.py --pretty

cd ../info-serve
env GOCACHE=/tmp/info-serve-go-build-cache go test ./...
```

回填结果：

```json
{
  "processed_count": 2733,
  "changed_count": 31,
  "status_counts": {
    "active": 269,
    "monitoring": 2464
  }
}
```

重启后接口复验：

| 检查项 | 结果 |
|---|---:|
| `/api/v1/events?category_code=all&status=active&page_size=50` 总数 | 198 |
| 前 50 条小红书来源 | 0 |
| 前 50 条微博来源 | 0 |
| 前 50 条主要分类 | `international` |
| `hot` active 总数 | 46 |

阶段判断：全网默认首页已从“最新内容流”调整为“可信公共事件优先”。`hot` 分类中的今日头条单源事件仍需要继续做摘要重写、事实源补偿和公共事件价值评分，但不再污染默认首页第一屏。

2026-05-14 hot 分类坏摘要兜底过滤：

继续验收 `hot` 分类时发现，虽然小红书/微博单源已经从可信流移除，但今日头条单源里仍有一批摘要属于原文拼接、营销语、话题标签或评论口吻，不适合直接作为“可信事件判断”展示。

修复内容：

1. `info-serve` active 可信流增加坏摘要兜底过滤。
2. 过滤典型坏摘要模式：`#` 话题标签、`全家都爱`、`巨巨`、`好喝`、`教会你`、`种草`、`我的观影报告`、`电影推荐`、`上头条`、`盘后，最大的`、`聊热点`、`划下红线`、`悲催了`、`快讯！` 等。
3. 该层作为 API 保险，不替代后续摘要重写；目的是先保证用户端可信流不展示明显低质表达。

验证：

```bash
cd info-serve
env GOCACHE=/tmp/info-serve-go-build-cache go test ./...
```

重启 `info-serve` 后复验：

| 检查项 | 结果 |
|---|---:|
| `hot` active 总数 | 37 |
| `hot` active 全量样本坏摘要命中 | 0 |
| `info-serve` 健康检查 | 通过 |

阶段判断：hot 分类已经从“有完整正文就展示”进一步收紧为“过滤明显坏摘要后展示”。剩余问题是摘要表达仍不够像情报判断，需要进入下一步：本地规则摘要重写 + LLM 摘要重写补偿。

2026-05-14 hot 分类规则摘要重写与可信流硬门槛：

继续推进用户端产品形态验收时，发现 hot active 事件里仍有少量半截话、评论腔、营销腔和占位句，例如事故类、体育赛程、公共政策会晤、媒体评论、科技产业动态被直接拼接为一句话摘要。

修复内容：

1. `info_aggregation` 规则分析增加公共事件标题重写：事故/救援/消防、辟谣、公共政策/会晤/经贸、体育赛程、民生经济、科技产业、媒体评论分别生成稳定情报句。
2. 修复 `_reanalyze_existing_event` 旧摘要未刷新到事件主表的问题，重分析后 `event.one_line_summary` 会同步更新。
3. `event_display_quality` 的错合并判断增加标题公共锚点识别，避免“广西公交车坠翻”和“广西梧州公交事故救援进展”这类同一公共事件被误判为串台。
4. 对低价值热点标题增加展示质量拦截，例如纯家庭/花絮型“接机”内容不进入可信热点流。
5. `info-serve` active 可信流增加占位摘要硬过滤：包含“已出现相关信息”的事件不再进入用户端可信流。

本地重分析结果：

| 操作 | 结果 |
|---|---:|
| hot active 规则重分析 | processed 55 / changed 9 |
| 媒体评论型摘要重刷 | processed 55 / changed 3 |
| 公共政策会晤与科技情绪摘要重刷 | processed 55 / changed 4 |
| active + monitoring 扩展重刷 | processed 160 / changed 157 |

最终验证：

```bash
cd info_aggregation
ENABLE_PUBLIC_API=1 ./.venv/bin/python -m pytest -q

cd ../info-serve
env GOCACHE=/tmp/info-serve-go-build-cache go test ./...
```

| 检查项 | 结果 |
|---|---:|
| `info_aggregation` 测试 | 263 passed |
| `info-serve` 测试 | 全部通过 |
| `info_aggregation` 健康检查 | 通过 |
| `info-serve` 健康检查 | 通过 |
| `hot` active 前 50 条返回数 | 47 |
| 坏摘要/占位句命中 | 0 |

阶段判断：用户端 hot 可信流已经具备“先能看、少噪声、像情报卡片”的基础形态。下一步重点不再堆功能，而是把 active 事件做成更有判断力的 AI 情报：事实链、反转提示、多源视角、历史关联和关注建议。

## 下一步

1. 继续补强 Reuters 详情采集，当前完整率仍为 0%，会直接拉低国际事件价值。
2. 基于预演工具建立每日事件聚合质量观察指标，跟踪单条组、多源组、错合并样本。
3. 继续深化大模型事件分析任务：新增事实校验任务、事件关联任务、专题摘要任务，并统一接入调用日志与 fallback。
4. 把“观察中”继续产品化：增加观察原因、人群讨论热度、事实源缺口提示和一键反馈。
